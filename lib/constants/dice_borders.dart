import 'package:flutter/material.dart';

abstract class Borders {
  static BorderRadius get diceBorderRadius => BorderRadius.circular(16);
  static BorderRadius get diceDotRadius => BorderRadius.circular(100);
}
